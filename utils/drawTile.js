//** Utility function to visualize grids
/*

function drawMap() {
	if (canvas.getContext) {
		for (let i = 0.6; i < canvasSize; i += 30) {
			context.moveTo(i, 0);
			context.lineTo(i, canvasSize);
		}

		for (let y = 0.6; y < canvasSize; y += 30) {
			context.moveTo(0, y);
			context.lineTo(canvasSize, y);
		}

		context.strokeStyle = 'gray';
		context.stroke();
	}
}

*/